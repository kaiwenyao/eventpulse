spring:
  jpa:
    hibernate:
      ddl-auto: validate
  kafka:
    listener:
      auto-startup: false

eventpulse:
  secret-key: test-secret-key-change-me-0123456789ab
  token-ttl-ms: 3600000

logging:
  level:
    org.hibernate.SQL: WARN
