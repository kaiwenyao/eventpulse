# EventPulse CRUD 丰富与主办方运营闭环规划

> 目标：将当前以演示为主的基础 CRUD，直接建设为完整的活动运营平台目标版本，覆盖主办方运营、参与者购票、逐票核销、消息可靠性、推荐、实时状态和数据分析。
>
> 规划日期：2026-09-01

## 1. 背景与现状

EventPulse 当前已经具备活动和预订的基础能力，但前后端能力不对称：

- 后端已经支持主办方创建活动、查询自己的活动、修改活动和取消活动。
- 后端会校验活动归属，主办方不能操作其他主办方的活动。
- 前端主办方页面目前只提供“标题、城市、发布”三个主要操作点。
- 前端没有活动编辑、详情、取消、复制、状态筛选、参与者管理等入口。
- 活动状态目前只有 `PUBLISHED` 和 `CANCELLED`，无法表达草稿、进行中、已结束和归档。
- 主办方无法查看订单和参与者，也无法了解活动的售票及转化情况。

因此，最终版本不应只增加几个按钮，而应完整覆盖下面的业务闭环：

```text
创建草稿 → 编辑预览 → 发布售票 → 管理订单与参与者 → 活动签到 → 结束归档
```

## 2. 建设范围

### 2.1 最终版本范围

1. 完善主办方活动的增、删、查、改。
2. 引入完整的活动生命周期和操作规则。
3. 增加主办方订单及参与者管理。
4. 丰富普通用户的活动查询和预订体验。
5. 补强权限、并发库存、错误码、审计和自动化测试。
6. 建设逐张票二维码核销、可靠消息、实时状态和完整通知中心。
7. 建设个性化推荐、附近活动、收藏、互动采集和运营分析。
8. 建设图片上传、活动封面管理和媒体资源生命周期。

### 2.2 暂不纳入

- 真实支付和自动退款。
- 复杂票种、座位图和动态定价。
- 多级组织、团队成员和精细化企业权限。
- 生产级财务结算与发票。

活动取消后如果已经存在订单，系统只记录取消状态并通知参与者；退款暂时标记为线下处理，不宣称已经自动退款。

## 3. 角色与权限

| 角色 | 可执行操作 |
| --- | --- |
| 访客 | 浏览、搜索、筛选活动，查看活动详情 |
| 普通用户 `USER` | 访客能力，以及预订、取消自己的预订、查看订单和消息、收藏活动 |
| 主办方 `ORGANISER` | 创建和管理自己的活动，查看自己活动的订单、参与者和运营数据 |

权限必须在后端强制校验，前端路由保护只能作为用户体验优化，不能代替后端鉴权。

主办方操作活动时必须同时满足：

1. 当前用户已登录。
2. 当前用户角色为 `ORGANISER`。
3. 目标活动的 `organiserId` 等于当前用户 ID。
4. 当前活动状态允许执行目标操作。

## 4. 主办方工作台信息架构

建议将当前单页表单扩展成以下页面：

| 路由 | 页面 | 主要职责 |
| --- | --- | --- |
| `/organiser` | 工作台 | 活动概览、近期活动、待处理事项、关键数据 |
| `/organiser/events` | 活动管理 | 查询、筛选、分页、状态查看和批量管理入口 |
| `/organiser/events/new` | 新建活动 | 创建并保存草稿或直接发布 |
| `/organiser/events/:id` | 活动运营详情 | 活动资料、状态、售票进度、快捷操作 |
| `/organiser/events/:id/edit` | 编辑活动 | 修改活动资料和售票设置 |
| `/organiser/events/:id/attendees` | 参与者管理 | 订单、参与者、签到和导出 |
| `/organiser/analytics` | 数据分析 | 浏览、点击、预订、售票率和转化率 |

主办方活动详情页使用一条生命周期轨道展示：

```text
草稿 → 已发布/售票中 → 进行中 → 已结束 → 已归档
              └──────→ 已取消
```

页面主要操作应随状态变化，例如草稿显示“继续编辑、预览、发布”，已发布显示“查看订单、取消活动”，已结束显示“查看数据、归档”。

## 5. 活动 CRUD 功能规划

### 5.1 创建活动 Create

活动表单建议包含以下字段：

#### 基本信息

- 活动标题。
- 简短摘要。
- 详细介绍。
- 活动分类。
- 封面图。

#### 时间地点

- 开始时间。
- 结束时间。
- 城市。
- 场地名称。
- 详细地址。
- 可选经纬度。

#### 售票设置

- 票价，继续使用整数分 `priceCents`。
- 总容量。
- 售票开始时间。
- 售票截止时间。
- 单次最多预订数量。

#### 参与说明

- 联系方式。
- 入场须知。
- 取消说明。

创建操作支持：

- 保存草稿。
- 发布前预览。
- 直接发布。
- 从已有活动复制创建。
- 表单字段校验。
- 存在未保存内容时的离开提醒。

业务规则：

- 新活动默认保存为 `DRAFT`，除非用户明确点击“发布活动”。
- 开始时间必须早于结束时间。
- 售票截止时间不能晚于活动开始时间。
- 容量必须大于零。
- 票价不能为负数。
- 前端不得提交 `organiserId`、`sold` 等服务端维护字段。

### 5.2 查询活动 Read

#### 主办方活动列表

支持：

- 按标题关键词搜索。
- 按状态筛选。
- 按分类筛选。
- 按开始日期区间筛选。
- 按最近更新、开始时间、售票量排序。
- 数据库分页。
- 展示状态、时间、城市、已售、容量、余票和售票进度。

每行提供与状态匹配的操作：

- 查看。
- 编辑。
- 预览。
- 复制。
- 发布。
- 取消。
- 归档。
- 删除草稿。

#### 主办方活动详情

详情页面至少展示：

- 活动完整资料。
- 当前生命周期状态。
- 已售数量、余票和售票率。
- 订单数和取消数。
- 最近订单或最近操作记录。
- 可执行操作及不能操作的原因。

### 5.3 修改活动 Update

主办方可以修改自己的活动，但不同状态的修改范围不同。

| 活动状态 | 允许修改 |
| --- | --- |
| `DRAFT` | 所有可编辑字段 |
| `PUBLISHED` | 文案、封面、地点、参与须知、未来时间、容量等受控字段 |
| `ONGOING` | 联系方式、参与须知等非交易字段 |
| `FINISHED` | 原则上只读，可补充归档备注 |
| `CANCELLED` | 只读，允许查看取消原因和订单影响 |
| `ARCHIVED` | 只读 |

通用限制：

- 容量不能小于已售票数。
- 修改活动时间或地点时，如果已经存在订单，应向主办方展示影响人数并通知参与者。
- 已结束或已取消活动不能重新进入售票状态。
- 使用乐观锁版本号避免两个页面同时编辑导致后保存的数据覆盖先保存的数据。
- 重要修改应记录操作审计。

### 5.4 删除、取消与归档 Delete

“删除”活动需要区分物理删除、业务取消和归档：

| 条件 | 处理方式 |
| --- | --- |
| 草稿且没有订单 | 允许永久删除 |
| 已发布且没有订单 | 允许取消，也可根据产品策略允许删除 |
| 已存在订单 | 禁止物理删除，只能取消 |
| 已结束 | 允许归档 |
| 已取消 | 保留记录，可归档，不物理删除 |

取消活动时需要：

1. 要求填写取消原因。
2. 展示受影响订单数和参与者人数。
3. 将活动状态设置为 `CANCELLED`。
4. 停止新的预订。
5. 保留活动和订单记录。
6. 给已有参与者生成站内通知。
7. 记录主办方、取消时间和取消原因。

## 6. 活动生命周期

建议将状态扩展为：

| 状态 | 含义 | 是否公开展示 | 是否允许预订 |
| --- | --- | --- | --- |
| `DRAFT` | 草稿 | 否 | 否 |
| `PUBLISHED` | 已发布/售票中 | 是 | 满足售票时间和库存时允许 |
| `ONGOING` | 活动进行中 | 是 | 否 |
| `FINISHED` | 已结束 | 可查看 | 否 |
| `CANCELLED` | 已取消 | 原链接可查看 | 否 |
| `ARCHIVED` | 已归档 | 默认不进入公开列表 | 否 |

允许的主要状态转换：

```text
DRAFT     → PUBLISHED
DRAFT     → 删除
PUBLISHED → CANCELLED
PUBLISHED → ONGOING
ONGOING   → FINISHED
FINISHED  → ARCHIVED
CANCELLED → ARCHIVED
```

不允许将 `CANCELLED` 或 `FINISHED` 直接恢复为 `PUBLISHED`。如果需要重复举办，应使用“复制活动”创建新活动。

## 7. 主办方订单与参与者 CRUD

### 7.1 查询订单与参与者

主办方只能查看自己活动关联的订单，支持：

- 按订单号搜索。
- 按参与者姓名或邮箱搜索。
- 按订单状态筛选。
- 按是否签到筛选。
- 按创建时间排序和分页。
- 查看总订单数、总票数、取消数和签到数。
- 导出 CSV。

参与者列表只返回运营需要的信息，不返回密码、JWT 或其他敏感字段。

### 7.2 逐张票核销与撤销核销

最终版本按单张票核销，不以整笔 booking 代替票据：

- 每个成功预订按照购买数量生成对应数量的独立票据。
- 每张票拥有不可预测且唯一的核销码和二维码。
- 主办方可以扫描二维码或手动输入核销码。
- 系统校验票据所属活动、票据状态和当前主办方权限。
- 防止同一张票重复核销，并向操作人员展示首次核销时间。
- 允许具有权限的主办方撤销误核销。
- 记录核销时间、核销设备或来源、操作者和撤销原因。
- booking 详情汇总展示总票数、已核销票数和未核销票数。
- 活动取消或订单取消后，对应票据立即失效。

### 7.3 订单修改边界

主办方不应随意修改用户订单数量或将取消订单恢复为确认状态。主办方可执行的操作限制为：

- 查询。
- 签到和撤销签到。
- 添加内部备注。
- 在活动取消流程中触发通知。

普通用户仍只能查看和取消自己的订单。

## 8. 普通用户侧 CRUD 丰富

### 8.1 活动查询

- 关键词搜索。
- 城市、分类、日期、价格和余票筛选。
- 按时间、热度、价格排序。
- 数据库分页。
- 收藏和取消收藏活动。
- 查看已取消活动的原因，但已取消活动不进入默认发现列表。

### 8.2 预订

- 活动详情页选择预订数量，不再固定为一张。
- 提交前展示活动、时间、数量和金额汇总。
- 预订成功后进入订单确认页。
- 库存不足时返回明确提示。
- 售票尚未开始、已截止、活动开始或活动取消时禁止预订。

### 8.3 我的订单

- 查询全部订单。
- 按即将开始、已结束、已取消分组。
- 查看订单详情。
- 在规则允许时取消订单。
- 查看签到状态。

### 8.4 消息中心

前端名称由“Kafka 消息”改为“消息中心”。Kafka 是内部实现，不应暴露给最终用户。

消息类型可以包括：

- 预订成功。
- 预订取消。
- 活动时间或地点变更。
- 活动取消。
- 活动即将开始。

## 9. 推荐 API 设计

为了区分公开活动目录和主办方管理能力，建议逐步将主办方接口收敛到 `/api/organiser` 命名空间。旧接口可在迁移期保留兼容。

### 9.1 活动管理

```http
GET    /api/organiser/events
POST   /api/organiser/events
GET    /api/organiser/events/{id}
PUT    /api/organiser/events/{id}
DELETE /api/organiser/events/{id}

POST   /api/organiser/events/{id}/publish
POST   /api/organiser/events/{id}/cancel
POST   /api/organiser/events/{id}/archive
POST   /api/organiser/events/{id}/duplicate
```

其中：

- `DELETE` 只用于允许物理删除的草稿。
- `cancel` 和 `archive` 是明确的业务动作，不使用 `DELETE` 混合表达。
- `PUT` 请求携带版本号，用于乐观锁冲突检测。

活动列表参数示例：

```http
GET /api/organiser/events?q=音乐&status=PUBLISHED&category=music&page=0&size=20&sort=updatedAt,desc
```

### 9.2 订单与参与者

```http
GET  /api/organiser/events/{id}/bookings
GET  /api/organiser/events/{id}/attendees
GET  /api/organiser/events/{id}/attendees.csv
POST /api/organiser/tickets/check-in
POST /api/organiser/tickets/{id}/undo-check-in
GET  /api/organiser/tickets/{code}
```

### 9.3 数据分析

```http
GET /api/organiser/dashboard
GET /api/organiser/events/{id}/analytics
GET /api/organiser/analytics?from=2026-09-01&to=2026-09-30
```

### 9.4 公开活动与用户接口

```http
GET    /api/events
GET    /api/events/{id}
POST   /api/events/{id}/favourite
DELETE /api/events/{id}/favourite
POST   /api/media/images
DELETE /api/media/images/{id}

POST   /api/bookings
GET    /api/bookings
GET    /api/bookings/{id}
POST   /api/bookings/{id}/cancel
GET    /api/bookings/{id}/tickets
```

## 10. 数据模型调整建议

### 10.1 `events` 新增字段

| 字段 | 用途 |
| --- | --- |
| `summary` | 活动简短摘要 |
| `venue_name` | 场地名称 |
| `address` | 详细地址 |
| `ends_at` | 结束时间 |
| `cover_url` | 封面图地址 |
| `sales_start_at` | 售票开始时间 |
| `sales_end_at` | 售票截止时间 |
| `max_quantity_per_booking` | 单次最大预订数量 |
| `contact_info` | 联系方式 |
| `attendance_notes` | 参与须知 |
| `cancellation_reason` | 活动取消原因 |
| `cancelled_at` | 活动取消时间 |
| `updated_at` | 最近更新时间 |
| `archived_at` | 归档时间 |
| `version` | 乐观锁版本号 |

### 10.2 `bookings` 与 `notifications` 调整

`bookings` 新增字段：

| 字段 | 用途 |
| --- | --- |
| `cancelled_at` | 用户取消时间 |
| `organiser_note` | 主办方内部备注 |

签到状态由关联 `tickets` 聚合得出，不在 booking 上重复保存签到事实。

`notifications` 从仅关联 booking 扩展为用户消息中心：

| 字段 | 用途 |
| --- | --- |
| `user_id` | 消息接收者 |
| `event_id` | 可空，关联活动 |
| `booking_id` | 可空，关联订单 |
| `type` | 预订、变更、取消、提醒、核销等消息类型 |
| `title` | 用户可读标题 |
| `message` | 用户可读正文 |
| `payload` | 页面跳转等结构化附加信息 |
| `dedup_key` | 消费幂等键 |
| `read_at` | 阅读时间 |
| `created_at` | 创建时间 |

### 10.3 新增表

#### 活动操作审计 `event_audit_logs`

```text
id
event_id
operator_id
action
before_data
after_data
created_at
```

记录发布、重要修改、取消、归档等操作。

#### 收藏 `event_favourites`

```text
user_id
event_id
created_at
UNIQUE(user_id, event_id)
```

#### 票据表 `tickets`

```text
id
booking_id
event_id
ticket_code_hash
status
checked_in_at
checked_in_by
check_in_source
revoked_at
revoked_by
revocation_reason
created_at
UNIQUE(ticket_code_hash)
```

`ticket_code_hash` 保存核销码的哈希值，不直接保存可用于入场的原始二维码密钥。票据状态至少包含 `VALID`、`CHECKED_IN`、`CANCELLED` 和 `REVOKED`。

#### 媒体资源表 `media_assets`

```text
id
owner_id
storage_key
public_url
content_type
size_bytes
status
created_at
deleted_at
```

活动封面通过受控上传接口生成，校验文件类型、大小和资源所有权。活动引用中的媒体不能被其他用户删除。

#### 偏好与互动

`user_preferences` 保存用户感兴趣的分类、城市和附近活动坐标；`interactions` 保存 `VIEW`、`CLICK`、`SAVE`、`UNSAVE`、`BOOK` 和 `CANCEL` 行为。客户端只能提交浏览、点击和收藏类行为，预订及取消行为由服务端可靠写入。

#### 推荐请求 `recommendation_requests`

保存请求 ID、用户、推荐分区、模型版本、特征版本、冻结候选、查询时间和过期时间，用于稳定翻页、解释推荐和离线评估。

#### 可靠消息 `outbox`

订单创建、订单取消、活动变更、活动取消和票据状态变化与对应 Outbox 事件在同一数据库事务内提交。Relay 发布成功后记录 `published_at`；消费者按 `dedup_key` 幂等处理。

#### 运营聚合 `event_daily_metrics`

按活动和日期聚合浏览、点击、收藏、订单、售票、取消和核销数据。原始事件仍是事实来源，聚合表只服务工作台与趋势查询，并支持重新计算。

## 11. 权限、错误码与数据安全

### 11.1 HTTP 状态码

建议区分：

| 状态码 | 场景 |
| --- | --- |
| `400` | 参数格式或业务输入错误 |
| `401` | 未登录或 token 无效 |
| `403` | 角色无权访问或不是资源所有者 |
| `404` | 活动或订单不存在 |
| `409` | 状态冲突、版本冲突、库存冲突 |
| `500` | 未处理的服务器错误 |

不要继续将所有业务异常统一返回 `400`。

### 11.2 前端路由

- 未登录访问 `/organiser/**` 时跳转登录页。
- 普通用户访问 `/organiser/**` 时展示无权限页面或返回首页。
- 主办方页面请求失败时必须展示明确错误，不能静默为空列表。

### 11.3 数据安全

- DTO 不返回密码。
- 日志不打印 JWT、密码和参与者敏感信息。
- 导出参与者数据前再次验证活动所有权。
- 所有查询都在 Repository 或 SQL 层带上 organiser 所有权约束，Service 层再次校验。

## 12. 库存与并发

当前直接读取并修改 `sold` 的方式在并发预订下存在超卖风险，应优先改成数据库条件更新：

```sql
UPDATE events
SET sold = sold + :quantity,
    updated_at = now()
WHERE id = :eventId
  AND status = 'PUBLISHED'
  AND sold + :quantity <= capacity;
```

更新行数为零时，根据活动状态和库存返回“活动不可预订”或“余票不足”。取消订单时使用受控的库存释放逻辑。

活动编辑使用 `version` 乐观锁：

- 读取活动时返回版本号。
- 更新时提交旧版本号。
- 版本不匹配返回 `409`，提示主办方刷新后重新编辑。

## 13. 测试与验收

### 13.1 后端测试

- 普通用户不能调用主办方接口。
- 主办方不能查询和修改其他主办方的活动。
- 草稿可以删除，有订单的活动不能物理删除。
- 非法状态转换被拒绝。
- 活动容量不能低于已售数量。
- 并发预订不会超卖。
- 重复签到被拒绝，撤销签到可正确恢复。
- 版本冲突返回 `409`。
- 活动取消后不能再预订，并生成参与者通知。

### 13.2 前端测试

- 主办方角色可以看到工作台入口。
- 普通用户不能进入主办方路由。
- 活动表单必填项、时间范围和容量校验正确。
- 活动列表搜索、状态筛选和分页正确。
- 编辑后列表和详情数据同步更新。
- 删除草稿和取消活动使用不同的确认文案。
- 有订单的活动取消前展示影响人数。
- API 失败时展示可操作的错误状态。

### 13.3 端到端验收流程

```text
主办方登录
→ 创建草稿
→ 编辑完整资料
→ 预览并发布
→ 普通用户搜索并预订 2 张
→ 主办方查看订单和参与者
→ 主办方执行签到
→ 主办方查看售票数据
→ 活动结束并归档
```

补充异常流程：

```text
主办方发布活动
→ 用户产生预订
→ 主办方取消活动
→ 新预订被禁止
→ 原订单保留
→ 用户消息中心收到取消通知
```

## 14. 分阶段实施计划

### 第一阶段：主办方活动 CRUD

范围：

- 主办方活动列表。
- 完整的新建和编辑表单。
- 草稿、发布、取消、归档状态。
- 活动详情和预览。
- 删除草稿和复制活动。
- 主办方前后端权限保护。
- 统一错误状态。

验收标准：主办方可以完整创建、查询、编辑、发布、取消和归档自己的活动，并且不能操作其他主办方的活动。

### 第二阶段：订单和参与者管理

范围：

- 活动订单列表。
- 参与者搜索、筛选和分页。
- 独立票据生成、二维码展示、逐票核销与撤销核销。
- CSV 导出。
- 活动变更和取消通知。
- 数据库原子库存更新。

验收标准：主办方能够准确回答“谁报名了、买了几张、订单是什么状态、谁已经签到”。

### 第三阶段：普通用户体验

范围：

- 丰富活动筛选和排序。
- 收藏活动。
- 自主选择预订数量。
- 订单确认和详情。
- 订单状态分组。
- 用户化的消息中心。

验收标准：普通用户能够顺畅完成查找活动、预订、查看订单、取消和接收通知的完整流程。

### 第四阶段：运营数据

范围：

- 主办方工作台指标。
- 浏览、点击、预订和售票率。
- 日期趋势和单活动漏斗。
- 待处理事项、低余票和临近活动提醒。

验收标准：主办方不仅能维护活动资料，还能判断活动当前表现和需要采取的运营动作。

### 第五阶段：智能化、实时性与可靠性

范围：

- 推荐和附近活动。
- Outbox 消息可靠性。
- SSE 实时状态提示。
- Redis 热门缓存。
- 更完整的指标与审计。

第五阶段完成后再进行全链路容量验证、安全检查、无障碍检查和发布验收。五个阶段全部完成才构成本规划定义的最终版本。

## 15. 最终版本交付范围与完成定义

最终版本必须同时交付以下能力，不以缩减范围的演示版本作为完成标准。

### 15.1 主办方运营

- 完整活动工作台、活动列表、详情、预览和全字段表单。
- `DRAFT`、`PUBLISHED`、`ONGOING`、`FINISHED`、`CANCELLED`、`ARCHIVED` 全生命周期。
- 新建、查询、编辑、复制、发布、取消、归档和删除草稿。
- 活动封面上传、替换、删除和资源所有权校验。
- 订单与参与者查询、筛选、分页和 CSV 导出。
- 独立票据二维码、逐票核销、撤销核销和完整操作记录。
- 活动浏览、点击、收藏、预订、售票率、签到率和转化漏斗。

### 15.2 参与者体验

- 活动搜索、组合筛选、排序、分页、附近活动和个性化推荐。
- 收藏、取消收藏、互动记录和偏好管理。
- 自主选择预订数量、库存确认、订单确认和订单详情。
- 每张票独立展示二维码与当前核销状态。
- 按状态管理订单，并在规则允许时取消。
- 消息中心接收预订、变更、取消、提醒和核销相关通知。
- SSE 提供订单和票据状态的实时提示，REST 始终作为事实来源。

### 15.3 平台可靠性

- 数据库条件更新保证并发预订不超卖。
- 乐观锁防止主办方编辑覆盖。
- Outbox 保证订单、活动通知和 Kafka 消息最终可达。
- 消费者幂等处理，避免重复通知和重复互动记录。
- Redis 缓存失败时能够回源 PostgreSQL。
- 401、403、404、409 和 500 错误语义明确。
- 活动及票据敏感操作具有审计记录。
- 日志、导出、媒体上传和二维码密钥满足安全约束。

### 15.4 工程质量

- 后端单元测试、Repository 集成测试和并发库存测试通过。
- 前端组件测试、权限路由测试和表单状态测试通过。
- Playwright 覆盖主办方发布、用户预订、逐票核销、活动取消和通知流程。
- Smoke Test 覆盖数据库、Kafka、Redis、Outbox、SSE 和媒体上传关键链路。
- 桌面端、平板端和移动端布局可用，支持键盘操作和清晰的焦点状态。
- 监控可以观察预订、取消、推荐、Outbox 堆积、缓存回源和核销失败。

### 15.5 最终验收场景

```text
主办方创建并编辑活动
→ 上传封面并保存草稿
→ 预览后发布
→ 用户通过搜索、附近或推荐发现活动
→ 用户收藏并预订多张票
→ 系统原子扣减库存、生成独立票据并可靠发送通知
→ 主办方查看订单、导出参与者并逐张核销二维码
→ 用户实时看到票据状态变化
→ 主办方查看售票和转化分析
→ 主办方变更或取消活动，所有受影响用户收到通知
→ 活动结束并归档，审计记录完整保留
```

只有上述端到端业务流程和异常流程全部通过，EventPulse 才达到本规划定义的最终交付版本。
